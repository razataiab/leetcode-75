class Solution 
{
    public:
        bool isPalindrome(string s) 
        {
            // string filtered;
            // for(char c : s)
            // {
            //     if (isalnum(c))
            //     {
            //         filtered += tolower(c);
            //     }
            // }

            // string a = filtered;
            // reverse(a.begin(), a.end());

            // return a==filtered;



            // string filtered;
            // for(char c : s)
            // {
            //     if (isalnum(c))
            //     {
            //         filtered += tolower(c);
            //     }
            // }

            // for(int i = 0, j = filtered.size(); i < j/2; i++)
            // {
            //     if(filtered[i] != filtered[j - 1 - i])
            //     {
            //         return false;
            //     }
            // }

            // return true;
        }
};